{
  "name": "s3-upload-demo",
  "version": "1.0.0",
  "description": "Demo for uploading images to amazon s3",
  "main": "index.js",
  "scripts": {
    "upload-images": "node scripts/upload-images.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "carami",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.939.0",
    "dotenv": "^10.0.0"
  }
}
